spring:
  h2:
    console:
      enabled: true
  aop:
    auto: true
    proxy-target-class: true
  application:
    name: caliber-email
  cloud:
    config:
      uri: ${CALIBER_CONFIG_SERVER_URL}
      fail-fast: true
